__pycache__/
*.pyc

# Screenshots from the capture tools. Kept out of the tree because they are
# large, they are regenerated on every UI change, and a stale one is worse
# than none.
review-*.png
shot-*.png

# Profile that launch.py builds for hand testing.
.profile/
.env*
node_modules/
.next/
next-env.d.ts
tsconfig.tsbuildinfo
.vercel
