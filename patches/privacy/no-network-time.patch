diff --git a/components/network_time/network_time_tracker.cc b/components/network_time/network_time_tracker.cc
index db90c2bcac0e8..ee1796efed35d 100644
--- a/components/network_time/network_time_tracker.cc
+++ b/components/network_time/network_time_tracker.cc
@@ -57,13 +57,11 @@
 
 namespace network_time {
 
-// Network time queries are enabled on Android and all desktop platforms except
-// Chrome OS, which uses tlsdated to set the system time.
-#if BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_IOS)
+// Network time queries are disabled on every platform. The query goes to
+// Google over plaintext HTTP on a schedule, so it is both a recurring beacon
+// carrying the client's address and an unauthenticated channel. Desktop
+// operating systems already keep the clock synchronised.
 BASE_FEATURE(kNetworkTimeServiceQuerying, base::FEATURE_DISABLED_BY_DEFAULT);
-#else
-BASE_FEATURE(kNetworkTimeServiceQuerying, base::FEATURE_ENABLED_BY_DEFAULT);
-#endif
 
 namespace {
 
