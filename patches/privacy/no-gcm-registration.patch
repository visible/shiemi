diff --git a/components/gcm_driver/features.cc b/components/gcm_driver/features.cc
index 5345f95611b51..edadf64e1786f 100644
--- a/components/gcm_driver/features.cc
+++ b/components/gcm_driver/features.cc
@@ -19,6 +19,10 @@ BASE_FEATURE(kInvalidateTokenFeature,
              "GCMTokenInvalidAfterDays",
              base::FEATURE_ENABLED_BY_DEFAULT);
 
+BASE_FEATURE(kGCMRegistration,
+             "GCMRegistration",
+             base::FEATURE_DISABLED_BY_DEFAULT);
+
 const char kParamNameTokenInvalidationPeriodDays[] =
     "token_invalidation_period";
 // A token invalidation period of 0 means the feature is disabled, and the
diff --git a/components/gcm_driver/features.h b/components/gcm_driver/features.h
index 889da7b007e9e..fa19cad659148 100644
--- a/components/gcm_driver/features.h
+++ b/components/gcm_driver/features.h
@@ -18,6 +18,9 @@ namespace features {
 BASE_DECLARE_FEATURE(kInvalidateTokenFeature);
 extern const char kParamNameTokenInvalidationPeriodDays[];
 
+// Whether the GCM client may register with and connect to the push service.
+BASE_DECLARE_FEATURE(kGCMRegistration);
+
 // The period after which the GCM token becomes stale.
 base::TimeDelta GetTokenInvalidationInterval();
 
diff --git a/components/gcm_driver/gcm_driver_desktop.cc b/components/gcm_driver/gcm_driver_desktop.cc
index 16e142aed302a..1b300380efa98 100644
--- a/components/gcm_driver/gcm_driver_desktop.cc
+++ b/components/gcm_driver/gcm_driver_desktop.cc
@@ -20,6 +20,7 @@
 #include "base/time/time.h"
 #include "base/timer/timer.h"
 #include "base/trace_event/trace_event.h"
+#include "components/gcm_driver/features.h"
 #include "components/gcm_driver/gcm_account_mapper.h"
 #include "components/gcm_driver/gcm_app_handler.h"
 #include "components/gcm_driver/gcm_client_factory.h"
@@ -1192,6 +1193,12 @@ GCMClient::Result GCMDriverDesktop::EnsureStarted(
   if (gcm_started_)
     return GCMClient::SUCCESS;
 
+  // Registering needs Google API keys this build does not carry, so starting
+  // the client only yields a failing request and a retry.
+  // --enable-features=GCMRegistration restores upstream behaviour.
+  if (!base::FeatureList::IsEnabled(features::kGCMRegistration))
+    return GCMClient::GCM_DISABLED;
+
   // Have any app requested the service?
   if (app_handlers().empty())
     return GCMClient::UNKNOWN_ERROR;
