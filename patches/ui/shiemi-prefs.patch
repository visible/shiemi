diff --git a/chrome/browser/extensions/api/settings_private/prefs_util.cc b/chrome/browser/extensions/api/settings_private/prefs_util.cc
index 09bec76bcb0bf..8c4b8b0cbc264 100644
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -285,6 +285,9 @@ const PrefsUtil::TypedPrefMap& PrefsUtil::GetAllowlistedKeys() {
       settings_api::PrefType::kBoolean;
 #endif
   (*s_allowlist)[::prefs::kShowHomeButton] = settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kContainedFullscreen] =
+      settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kDomainOnlyUrl] = settings_api::PrefType::kBoolean;
   (*s_allowlist)[contextual_search::kDriveConsentState] =
       settings_api::PrefType::kNumber;
   (*s_allowlist)[::prefs::kShowForwardButton] =
diff --git a/chrome/browser/ui/browser_ui_prefs.cc b/chrome/browser/ui/browser_ui_prefs.cc
index aa327f72a8864..bcdbe603f1dfb 100644
--- a/chrome/browser/ui/browser_ui_prefs.cc
+++ b/chrome/browser/ui/browser_ui_prefs.cc
@@ -190,6 +190,9 @@ void RegisterBrowserUserPrefs(user_prefs::PrefRegistrySyncable* registry) {
   registry->RegisterBooleanPref(prefs::kFullscreenAllowed, true);
 #endif
 
+  registry->RegisterBooleanPref(prefs::kContainedFullscreen, false);
+  registry->RegisterBooleanPref(prefs::kDomainOnlyUrl, false);
+
 #if BUILDFLAG(IS_CHROMEOS)
   registry->RegisterBooleanPref(prefs::kForceMaximizeOnFirstRun, false);
 #endif
diff --git a/chrome/common/pref_names.h b/chrome/common/pref_names.h
index 7f2ccebd7e501..dd9857a7afc36 100644
--- a/chrome/common/pref_names.h
+++ b/chrome/common/pref_names.h
@@ -1045,6 +1045,15 @@ inline constexpr char kMessageCenterDisabledExtensionIds[] =
 // platforms.
 inline constexpr char kFullscreenAllowed[] = "fullscreen.allowed";
 
+// Boolean pref. When true, a page asking for fullscreen fills the tab's
+// content area instead of the screen, leaving the window and the rest of the
+// desktop alone.
+inline constexpr char kContainedFullscreen[] = "shiemi.contained_fullscreen";
+
+// Boolean pref. When true, the omnibox shows only the site's host while it is
+// not being edited. Focusing it still reveals the whole URL.
+inline constexpr char kDomainOnlyUrl[] = "shiemi.domain_only_url";
+
 #if BUILDFLAG(IS_ANDROID)
 // The user requested font weight adjustment from OS-level settings.
 // Exposed only to mobile Android.
