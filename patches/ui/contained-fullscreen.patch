diff --git a/chrome/browser/extensions/api/settings_private/prefs_util.cc b/chrome/browser/extensions/api/settings_private/prefs_util.cc
index 09bec76bcb0bf..4e9a437ac4d7d 100644
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -285,6 +285,8 @@ const PrefsUtil::TypedPrefMap& PrefsUtil::GetAllowlistedKeys() {
       settings_api::PrefType::kBoolean;
 #endif
   (*s_allowlist)[::prefs::kShowHomeButton] = settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kContainedFullscreen] =
+      settings_api::PrefType::kBoolean;
   (*s_allowlist)[contextual_search::kDriveConsentState] =
       settings_api::PrefType::kNumber;
   (*s_allowlist)[::prefs::kShowForwardButton] =
diff --git a/chrome/browser/resources/settings/appearance_page/appearance_page.html b/chrome/browser/resources/settings/appearance_page/appearance_page.html
index b40d1a7199fa8..4690a145f45b3 100644
--- a/chrome/browser/resources/settings/appearance_page/appearance_page.html
+++ b/chrome/browser/resources/settings/appearance_page/appearance_page.html
@@ -240,6 +240,12 @@
             on-change="onShowTabSearchButtonChange_">
         </settings-toggle-button>
 
+        <settings-toggle-button class="hr" id="containedFullscreen"
+            pref="{{prefs.shiemi.contained_fullscreen}}"
+            label="$i18n{containedFullscreen}"
+            sub-label="$i18n{containedFullscreenDesc}">
+        </settings-toggle-button>
+
         <settings-toggle-button class="hr" id="showSavedTabGroups"
             pref="{{prefs.bookmark_bar.show_tab_groups}}"
             label="$i18n{showTabGroupsInBookmarksBar}"
diff --git a/chrome/browser/ui/browser_ui_prefs.cc b/chrome/browser/ui/browser_ui_prefs.cc
index aa327f72a8864..9ccbdfd87cdb4 100644
--- a/chrome/browser/ui/browser_ui_prefs.cc
+++ b/chrome/browser/ui/browser_ui_prefs.cc
@@ -190,6 +190,8 @@ void RegisterBrowserUserPrefs(user_prefs::PrefRegistrySyncable* registry) {
   registry->RegisterBooleanPref(prefs::kFullscreenAllowed, true);
 #endif
 
+  registry->RegisterBooleanPref(prefs::kContainedFullscreen, false);
+
 #if BUILDFLAG(IS_CHROMEOS)
   registry->RegisterBooleanPref(prefs::kForceMaximizeOnFirstRun, false);
 #endif
diff --git a/chrome/browser/ui/exclusive_access/fullscreen_controller.cc b/chrome/browser/ui/exclusive_access/fullscreen_controller.cc
index f597f2282a9e9..0c19ed375b04e 100644
--- a/chrome/browser/ui/exclusive_access/fullscreen_controller.cc
+++ b/chrome/browser/ui/exclusive_access/fullscreen_controller.cc
@@ -22,8 +22,10 @@
 #include "chrome/browser/ui/exclusive_access/exclusive_access_manager.h"
 #include "chrome/browser/ui/exclusive_access/fullscreen_tab_params.h"
 #include "chrome/browser/ui/exclusive_access/fullscreen_within_tab_helper.h"
+#include "chrome/common/pref_names.h"
 #include "components/history/core/browser/history_service.h"
 #include "components/history/core/browser/history_types.h"
+#include "components/prefs/pref_service.h"
 #include "content/public/browser/fullscreen_types.h"
 #include "content/public/browser/navigation_details.h"
 #include "content/public/browser/navigation_entry.h"
@@ -40,11 +42,6 @@
 #include "ui/display/screen.h"
 #include "ui/display/types/display_constants.h"
 
-#if !BUILDFLAG(IS_MAC)
-#include "chrome/common/pref_names.h"
-#include "components/prefs/pref_service.h"
-#endif
-
 #if !BUILDFLAG(IS_ANDROID)
 #include "chrome/browser/ui/blocked_content/popunder_preventer.h"
 #endif  // !BUILDFLAG(IS_ANDROID)
@@ -654,7 +651,15 @@ bool FullscreenController::MaybeToggleFullscreenWithinTab(
   }
 
   if (enter_fullscreen) {
-    if (web_contents->IsBeingVisiblyCaptured()) {
+    // Contained fullscreen rides on the path capture already uses: the page
+    // goes fullscreen into the tab's content area and the window is left
+    // alone. Excluded once the tab holds real fullscreen, so a nested request
+    // stays on the path it started on.
+    Profile* const profile = exclusive_access_manager()->context()->GetProfile();
+    const bool contained =
+        profile && profile->GetPrefs()->GetBoolean(prefs::kContainedFullscreen) &&
+        exclusive_access_tab() != web_contents;
+    if (web_contents->IsBeingVisiblyCaptured() || contained) {
       FullscreenWithinTabHelper::CreateForWebContents(web_contents);
       FullscreenWithinTabHelper::FromWebContents(web_contents)
           ->SetIsFullscreenWithinTab(true);
diff --git a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
index d21c249464c61..e789e66ed3271 100644
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -634,6 +634,16 @@ void AddAppearanceStrings(content::WebUIDataSource* html_source,
   };
   html_source->AddLocalizedStrings(kLocalizedStrings);
 
+  // Our own strings, not upstream's, so they are not in a .grd: settings_strings
+  // is rewritten wholesale at build time to carry the product name, and a hunk
+  // in there would be discarded by that step.
+  html_source->AddString("containedFullscreen",
+                         u"Keep full screen inside the window");
+  html_source->AddString(
+      "containedFullscreenDesc",
+      u"A video going full screen fills the tab rather than the whole display, "
+      u"so the rest of your desktop stays where it is");
+
   html_source->AddBoolean("ntpSimplificationBookmarksBarEnabled",
                           base::FeatureList::IsEnabled(
                               ntp_features::kNtpSimplificationBookmarkBar));
diff --git a/chrome/common/pref_names.h b/chrome/common/pref_names.h
index 7f2ccebd7e501..ac6146716fe06 100644
--- a/chrome/common/pref_names.h
+++ b/chrome/common/pref_names.h
@@ -1045,6 +1045,11 @@ inline constexpr char kMessageCenterDisabledExtensionIds[] =
 // platforms.
 inline constexpr char kFullscreenAllowed[] = "fullscreen.allowed";
 
+// Boolean pref. When true, a page asking for fullscreen fills the tab's
+// content area instead of the screen, leaving the window and the rest of the
+// desktop alone.
+inline constexpr char kContainedFullscreen[] = "shiemi.contained_fullscreen";
+
 #if BUILDFLAG(IS_ANDROID)
 // The user requested font weight adjustment from OS-level settings.
 // Exposed only to mobile Android.
