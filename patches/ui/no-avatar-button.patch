diff --git a/chrome/browser/ui/views/toolbar/avatar_toolbar_button_interface.cc b/chrome/browser/ui/views/toolbar/avatar_toolbar_button_interface.cc
index 7f3e55f684fb7..794705179fe9e 100644
--- a/chrome/browser/ui/views/toolbar/avatar_toolbar_button_interface.cc
+++ b/chrome/browser/ui/views/toolbar/avatar_toolbar_button_interface.cc
@@ -20,8 +20,11 @@ bool AvatarToolbarButtonInterface::CanShowForProfile(Profile* profile) {
           profile->GetOTRProfileID().IsCaptivePortal());
 #else
   // DevTools profiles are OffTheRecord, so hide it there.
-  return profile->IsIncognitoProfile() || profile->IsGuestSession() ||
-         profile->IsRegularProfile();
+  //
+  // Regular profiles are dropped from the upstream list: signed out, the button
+  // is a sign-in entry point wearing a profile icon. Incognito and guest keep
+  // it, where it is the only thing in the window saying which mode this is.
+  return profile->IsIncognitoProfile() || profile->IsGuestSession();
 #endif
 }
 
